<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link rel="stylesheet" href="{{ asset('styles.css') }}">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        <nav class="navbar">
            <div class="navbar-inner">
                <a href="{{ path('app_home') }}" class="navbar-brand">Pokémonu meklētājs</a>
                <div class="navbar-links">
                    <a href="{{ path('app_home') }}" class="nav-link">Sākums</a>
                    <a href="{{ path('app_pokemon_list') }}" class="nav-link">Pokémoni</a>
                    <a href="{{ path('app_favorites_index') }}" class="nav-link">Favorīti{% if favoritesCount > 0 %} ({{ favoritesCount }}){% endif %}</a>
                </div>
            </div>
        </nav>
        {% block body %}{% endblock %}
    </body>
</html>
